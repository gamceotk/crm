# === GAM CRM — environment template ===
# Real values live in website/.env.local (gitignored). Never commit secrets.

# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Optional integrations
GEMINI_API_KEY=
